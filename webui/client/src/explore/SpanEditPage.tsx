/* Explore › Span edit — placeholder until the page is built in this run. */
import { Skeleton } from '../shell/Skeleton'
export function SpanEditPage({ memberId }: { memberId: string }) { return <Skeleton id="explore.span-edit" key={memberId} /> }
