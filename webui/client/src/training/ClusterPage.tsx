/* analyse.training.cluster — placeholder while the page is being built (replaced in the next commit). */
import { EmptyState, Page } from '../kit'
import { Header } from '../shell/Header'

export function ClusterPage() {
  return (
    <>
      <Header workspace="Analyse" page="03 Cluster" subtitle="being built" />
      <Page testid="training-cluster">
        <EmptyState icon="hourglass" title="03 Cluster is being built" caption="the page lands in the next commit" />
      </Page>
    </>
  )
}
