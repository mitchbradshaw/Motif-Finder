/* analyse.training.encode — placeholder while the page is being built (replaced in the next commit). */
import { EmptyState, Page } from '../kit'
import { Header } from '../shell/Header'

export function EncodePage() {
  return (
    <>
      <Header workspace="Analyse" page="04 Encode" subtitle="being built" />
      <Page testid="training-encode">
        <EmptyState icon="hourglass" title="04 Encode is being built" caption="the page lands in the next commit" />
      </Page>
    </>
  )
}
