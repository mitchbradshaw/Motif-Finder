# Discovery — build status

| page | status | what's left | last commit |
|---|---|---|---|
| discovery.runs | done | polish only (frame 1b channel checkboxes read-only by design) | runs commit |
| discovery.seed | done | matches pager keyboard; histogram drag is pointer-only | seed commit |
| discovery.compare | done | keyboard stepping (←/→) not bound; hover readout is pointer-only | compare commit |
| discovery.stages | done | table-row hover highlights cards; no keyboard stepping | stages commit |

All four pages are in `webui/smoke_pages/discovery.json` (42 states, 0 failures against the Vite dev server).
