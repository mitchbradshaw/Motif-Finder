# pytest baseline after the Panel tree was retired — main @ 6218904 — 2026-09-21
# pytest -n auto -q -p no:cacheprovider from the repo root (main checkout), 130.76 s on this machine (208.66 s serial, same result)
#
# 969 collected: 954 passed, 13 skipped, 2 failed
#
# The 39 failures of the previous baseline (7dec6e7: the LibraryGrid(conn) contract mismatch and the
# Windows WinError 32 teardown locks) were all in files that imported the Panel tree; every one of
# those files was deleted or cut in commits 74dca6d / 9ce1fd8 (tag archive/panel-ui holds them), so
# the failure set attributable to the code is now EMPTY.
#
# The two failures and thirteen skips below are ENVIRONMENTAL: this checkout's DATA/ has no
# DATA/derived/channels/ (and no DATA/db/annotations.sqlite) as of 2026-09-21 12:27. The thirteen
# skips are the _channel_available() guards in tests/test_execution.py doing their job; the two
# failures are drop-motif tests that read Fig2A channel .npy files without a guard. With the derived
# channels present both pass (they were green in the 7dec6e7 run). Compare failure SETS against
# this list, not counts.
FAILED tests/test_drop_motifs_defects10.py::test_ch4_window05_second_visible_drop_is_detected - FileNotFoundError: DATA/derived/channels/Fig2A_dt0p1/CH4.npy (data absent on this machine)
FAILED tests/test_drop_motifs_defects10.py::test_ch1_the_0p172_mv_drop_at_904_6s_is_detected - FileNotFoundError: DATA/derived/channels/Fig2A_dt0p1/CH1.npy (data absent on this machine)
#
# tests/test_webui_api.py (12 tests) needs FastAPI, which lives only in webui/.venv; it skips under
# the conda pytest with a message and passes under:  webui/.venv/Scripts/python.exe -m pytest tests/test_webui_api.py
