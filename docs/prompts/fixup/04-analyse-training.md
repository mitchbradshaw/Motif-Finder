# Fixup 04 — Analyse › Training: the training chain and blocks 01–05

**Status: skeleton.** Symptoms only. The prompt body is written after `QUESTIONS.md` Q-T1…Q-T2.

## Symptoms

| # | Symptom | Evidence |
|---|---|---|
| T1 | **All five Training pages are fixture-backed and still wear the "demo data" chip.** The server side exists — `preprocessing.sliding_windows` with the P12 guards, the training chain, `POST /api/windowsets` — but the pages are built on fixture shapes (dendrogram, contingency, stability, k-sweep) that no single run payload supplies. Making them honest needs each page to attach to a `windows_model` job and draw the WindowSet / Grouping / Model payloads through `charts/`. Wiring's estimate: 1–1.5 days. | wiring `reports/01-analyse-blocks.md` Left; `reports/05-review.md` §7 |
| T2 | **The sliding-windows features plot can be bigger.** A layout complaint about Training › 02 Window matrix (features per window) — the panel is too small to read at the size the page gives it. | user |
| T3 | **The hierarchical-cluster (03) and classifier (05) blocks need work — diagnosed, and the cause is upstream of the pages.** `Working/Catalogue/dendrogram/dendrogram_cluster.py::cluster_window_matrix` returns a `ClusterResult` carrying the scipy `linkage_matrix`, `leaf_order`, `df_membership`, `cophenetic_r` and `silhouette`. `Adapters/catalogue_cluster.py:69-79` keeps only `linkage` (the *method string*), `k`, `cluster_counts`, `n_windows`, `n_features` and drops the rest. Consequences, in order: **(i)** no linkage matrix reaches any payload, so nothing can draw a dendrogram — `serialize.py::_grouping` has no branch for one and `Renderer.tsx:252` prints `· ward` as text; `GroupingR` draws a flat colour strip that a k-means result would render identically. **(ii)** `k` is an editable `ParamSpec` but `silhouette` and `cophenetic_r` are dropped, so there is **no criterion, no k-sweep and no stability number** in any payload. **(iii)** cluster members are half-visible: `labels[:5000]` ships, `df_membership` is dropped, there is no members-of-cluster-k list, and past 5,000 windows the strip truncates with `capped: true` that `GroupingR` never surfaces. **(iv)** `ClusterPage.tsx` already has a real hand-rolled dendrogram with an interactive cut and a k-sweep panel — **drawn entirely from the fixture**. The UI exists and is wired to nothing. **The classifier:** the joblib is write-only — `catalogue_classifier.py:229` dumps a `VarianceThreshold + RandomForestClassifier` pipeline, `_persist` registers it, and **nothing in the repo ever loads it back** (`window_matrix`'s `rf_model_path` expects a different artifact shape — a `ThresholdedPipeline` over a raw signal window, not a feature vector). Its entire evaluation is one `holdout_accuracy`: no confusion matrix, no per-class accuracy, no `feature_importances_` (computed inside the fitted forest and never read), no shuffle null — though `Renderer.tsx:276` promises "Models judges it against a label-shuffle null". `_run` spreads `**params` flat into meta while `serialize.py:281-282`'s `keep` tuple whitelists a `"params"` key that is never present, so `n_estimators`, `class_weight`, `holdout_frac` and `random_state` are dropped from the model card. And `_split` silently returns "train on everything" when a class has fewer than two members, making `holdout_accuracy` `None` — which `ModelR` prints as `holdout accuracy —` with no reason given. Both blocks' `estimate` returns `None` by design, so a training chain is always priced "unknown". | user; `Adapters/catalogue_cluster.py:69-79`, `Adapters/catalogue_classifier.py:143-149,229,232-235,259`, `webui/server/serialize.py:260-274,281-282`, `webui/client/src/api/training.ts:1-3,92-97`, `analyse/Renderer.tsx:241-262,268,276` |
| T4 | **`window_sets` holds zero rows and nothing writes one.** Saving a set is Analyse's act (P18: any block outputting a `WindowSet` offers *Save window set*); the table, the route and the Library page are built. Everything downstream that needs a window set — Library › Window sets, Review's `training-windows` and `model-verification` queues, Models › Launch from a window set — is therefore untestable on real data. | wiring `reports/03-library.md` §9; `reports/05-review.md` §12.6 |

## Goal · Work · Testing and critique · Report

*(written after the questions)*
