# Fixup 05 — Discovery: Runs, Seed search, Compare, Compare every stage

**Status: skeleton.** Symptoms only. The prompt body is written after `QUESTIONS.md` Q-D1…Q-D3.

## Symptoms

| # | Symptom | Evidence |
|---|---|---|
| D1 | **Adding a template to the Runs page glitches — diagnosed.** The add flow is genuinely a POST (`AddTemplateModal.tsx:81-99` → `POST /api/discovery/templates/apply` → `onClose()` → `await dx.reload()`); no optimistic row exists, so the critic-era defect really is gone. **The glitch is a layout one.** `RunsPage.tsx:60` renders the 600 px loading card as a **sibling** of the page content, not an else-branch: `{dx.loading && <Loading height={600}/>}` then `{dx.scope && (…content…)}`. `useSourced` keeps `data` and sets `loading: true` on reload, so the content is not replaced — a 600 px block is *inserted above it*, shoving the page down and back. `session.ts:116-122` then polls `base.reload()` **every 2 s** for as long as any run is `running`/`queued`, so the card flashes in and out every two seconds for the whole life of the run you just started — which is exactly why it only appears after you add one. Same pattern in `ComparePage.tsx:91`, `SeedPage.tsx:116`, `StagesPage.tsx:78`. **Three smaller faults in the same modal:** `sel` is initialised to `['mp_discord_v3','spike_shape_v1']` and `focusQ` to `'mp_discord_v3'` — **none of those templates exist** in the live registry, so the modal opens reading "0 templates selected" with both buttons disabled and the phantom names never cleared; `sel` is never reset after a successful add; and `inSessionReason` matches `r.key === t.name` while `_next_key` gives the first run the bare template name, so **applying a template once makes it un-selectable for the rest of the session**, even against a different scope. Also `dx.setScope` writes client state only — no `PUT` for channels/section — so a locally edited scope is what gets POSTed and is lost on reload. | user; `webui/client/src/discovery/RunsPage.tsx:60`, `discovery/session.ts:116-122`, `discovery/AddTemplateModal.tsx:19-20,30,81-99`, `api/seam.ts:39`, `webui/server/discovery.py:1449-1455` |
| D2 | **Precision is 0.00 by construction and it is not the algorithm's fault.** 11,234 of 11,269 annotations are fixed 600-sample 10-minute CNN windows — window labels, not event spans — while a real drop runs 21–4,875 samples (median 179). Under §4.6's IoU rule a 179-sample event against a 600-sample window reaches IoU 0.30 and can never count. Over the whole database, 80 of 732 detections are even width-compatible and **none** matches. The scoreboard says so beside the number. The three ways out are in wiring's questions table; **none has been chosen**. | wiring `reports/04-discovery.md` §"The finding this run produced" |
| D3 | **Settings › Nulls names a method the block does not implement.** The page offers *circular shift of the channel*; `preprocessing.surrogate` implements `phase_randomize` and `block_shuffle` only, and `resolve_null` now refuses it out loud. Wiring's note: *the chip on the page should probably change rather than the block* — a circular shift is a degenerate null for a shape search anyway. | wiring `reports/04-discovery.md` questions table |
| D4 | **A paired null per Discovery run is one surrogate realisation, not §9.4's 200.** The row states `null_draws` so the number is not read as an expectation. 3 h, and N× the sweep cost. | wiring `reports/04-discovery.md` Left |
| D5 | **§9.4's `alpha` and the multiple-comparison correction never reach `recommended_cut`.** They are Settings keys the seeded search does not read; the marker is computed at a hard-coded α = 0.01 with no correction. The page should at least say "correction: none". 2 h. | wiring `reports/04-discovery.md` Left |
| D6 | **The seed page's progress strip is `kit/sim`.** The search is a real job with real per-channel progress on `GET /api/jobs/{id}/events`; the page polls the result instead of subscribing, so the strip's *timing* is simulated while its *outcome* is not. `subscribeJob` already exists. 3 h. | wiring `reports/04-discovery.md` Left |
| D7 | ***Analyse events →* and *Open in Analyse* do nothing** — they need Analyse's source picker to accept a `SpanSet` from a Discovery run. 3 h once the picker takes one. | wiring `reports/04-discovery.md` Left |
| D8 | `?state=failed` is a display state; a real failure cannot be provoked from a URL. Declared, covered by route tests and the loud-failure card. | wiring `reports/04-discovery.md` Left |

## Goal · Work · Testing and critique · Report

*(written after the questions)*
