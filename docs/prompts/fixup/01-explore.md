# Fixup 01 — Explore: Corpus, Signal, Cross-channel, Span edit

**Status: skeleton.** Symptoms only. The prompt body is written after `QUESTIONS.md` Q-E1…Q-E4.

## Symptoms

| # | Symptom | Evidence |
|---|---|---|
| E1 | **[FIXED — fixup-a 13 + 14, `44fe2ed`.** The rail is one `MultiPick` over the live vocabulary now, and all four false claims are gone — plus three more the item did not name: the `demo` chips on *reviewed coverage* and *unreviewed only* (the same live read off `reviewed_spans`, 11,265 rows) and on the `matching` foot. The page's own docstring said it too and is rewritten. Smoke pins it: *no demo chip is left on the Corpus rail*, *the tag list offers 34 live vocabulary terms*.**] **Morphology tag selection should be a dropdown — and the cause behind the note is worse than the note.** The rail renders one `Checkbox` per tag over a 36-term vocabulary, which is why a dropdown was asked for (the rail's own `MultiPick`, already used for Runs and Methods, is the fix). But **the same control asserts four times that this database has no tags**: a `DemoTag` chip, the info text *"This database has no tags yet"*, the caption *"no tags in this database · demo counts"*, and the module docstring. All four are false. `getCorpusLive` already fetches `GET /api/channels/{id}/tags` for every channel and returns the vocabulary and per-channel counts with `source: 'live'`. Verified read-only against `DATA/db/annotations.sqlite` on 2026-09-22: `tag_vocabulary` holds **36 terms across 8 categories**, `annotation_tags` **11,319 rows**, `motif_entry_tags` **13,349**. | user; `webui/client/src/explore/RightRail.tsx:3-4,51-55`, `api/explore.ts:47-70`, `explore/bits.tsx:13` |
| E2 | **Sending a span to Review carries no information about it.** *Take span for Review* writes the span, but the researcher cannot say what they think it is (morphology, class, note, why it is interesting) at the moment of sending — so Review receives an unlabelled span and the context that made it worth sending is lost. | user |
| E3 | **[FIXED — fixup-a 15, `44fe2ed`.** The map goes through `getCoverageFiltered` with the rail's runs and methods, resolved to run ids (the route's `method=` is one substring, both pickers are multi-select); an empty intersection sends `run=-1`, which empties the detection layers rather than quietly showing every run. The note is deleted. `run_methods` moved out of `explore_routes.py` into `corpus.py` so the filtered path no longer drags FastAPI in and can be tested headless.**] **The Corpus page still fetches the unfiltered coverage map.** `coverage?run=&method=` is served and the rail's run list is live, but Corpus ignores both; a note beside the filters admits it. | wiring `reports/01-analyse-blocks.md` Left |
| E4 | **Cross-channel has no user window control.** Partly addressed by hand in commit `ab86d3a`: the anchor is now the channel's most *notable* span by a verdict preference order (`seed` › `interesting` › `artifact` › `unsure`) rather than the earliest — which on every `M2_aug` channel was a bulk `imported_10min` block deliberately marked `not_interesting` — and an anchor longer than 600 s is clamped to its first ten minutes, because honouring `M2_concat_fs1` CH4's 20-hour `interesting` annotation asked the route to cross-correlate 72,000 samples against fifteen siblings and never returned. The whole-channel option is still disabled with a reason, and the *user* still cannot choose the window. | wiring `reports/01-analyse-blocks.md` questions table; commit `ab86d3a` |
| E5 | **[FIXED — fixup-a 16, `44fe2ed`.** Labels only: the route's docstring and the new `COLOUR_BY_LABEL` ("annotations + detections"). The computation is untouched and two tests pin it as a SUM, so a later reading of the word "both" cannot turn it into an intersection.**] **`coverage.rows[].both` is a sum**, not an intersection: `"both": (ah + dh).tolist()` — annotation spans **plus** detection spans per bin. The page's colour-by means the sum, so the number is right for the colour and wrong as a label; the route's own docstring makes the same claim. | wiring `reports/01-analyse-blocks.md` critics, Left; `webui/server/corpus.py:143,187` |
| E6 | **Clicking a cluster on a plot does not open its waveforms.** The user wants a plot to be an index into the data, not a picture of it — on Explore's maps as well as in the Library. | user (see also `08-library.md` L7) |
| E7 | `api/explore.ts` still holds 2 fixture reads — the Span-edit page and the keyboard map. Honest: nothing computes either yet. | wiring `reports/05-review.md` §7; `api/explore.ts:10` |
| E8 | **[FIXED — fixup-a 17, `b8ed1c5`.** `decollideTicks` drops any tick whose label box would overlap the one before it, on every axis rather than only the `ends` one, using the label's own character count against the axis font; `axisAnchor` is shared with the renderer so the estimate and the rendering cannot disagree. Smoke measures it in a real browser (`getBoundingClientRect`) on Corpus, Signal and the Analyse chain.**] **Time-axis labels collide, and not only on Signal.** `TimeAxis` de-collides *only* when `ends` is true, and then only against the two ends with a 48 px guard; in the default path there is no collision test at all. `d3`'s `.ticks(n)` picks positions without knowing how wide `fmtAxis` renders them, so wide labels (`291.9 h`) overlap on a narrow surface. The `anchor` logic stops a label rendering half off the surface and does nothing about label-versus-label. It is a shared primitive, so Analyse has it too. | wiring `reports/01-analyse-blocks.md` critics (P2-2); `webui/client/src/charts/primitives.tsx:14-36`, `charts/scale.ts:18-22` |

## Goal · Work · Testing and critique · Report

*(written after the questions)*
