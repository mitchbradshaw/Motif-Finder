# Fixup 02 — Analyse: Chain, Block, Algorithm glyphs

**Status: skeleton.** Symptoms only. The prompt body is written after `QUESTIONS.md` Q-A1…Q-A5.

## Symptoms

| # | Symptom | Evidence |
|---|---|---|
| A1 | **Analyse should open to an empty chain.** `DEFAULT_CHAIN` is a prefilled three-stage `mp_threshold` chain, persisted to `sessionStorage`, so the page never opens empty and the first thing a new user sees is somebody else's recipe. Changing it touches the empty-chain validation path, the Insert-a-stage affordance, and the smoke states that assume three rows. | user; `webui/client/src/state.tsx:77-84` |
| A2 | **[PARTLY FIXED — fixup-a 1–3, `4176712` / `4d7446f` / `93c107d`.** (i) `serialize.py` nanmeans its image blocks and MARKS the cells that hold no data instead of painting them black under a fabricated `value_range: [0, 1]`; an entirely-NaN image says so in words. (ii) the block declares `max_span_samples = 65_000`, derived from the gramian blocks' 200 MB budget at 3,072 peak live bytes per span sample, and is on `_complexity`'s heavy list. The end-index disagreement is fixed too: `summation_threshold` speaks `SpanSet`'s half-open form, for its spans and its `pseudo_spikes` alike. **(iii) is NOT fixed** — `_spanset` still has no case for `meta["pseudo_spikes"]`, so the paper's pseudo-spike set still never reaches the client. Evidence for the rest: `webui/screenshots/fixup/A/`.**] **The Dehshibi template is broken — diagnosed, three causes, none of them a type mismatch.** The three blocks are type-consistent (`signal → encoding → scores → spanset`) and the executor threads them correctly. **(i) The stage-1 image is always black on a real span.** `preprocessing.wavelet_transform` allocates `g_all = np.full((64, n), nan)` and fills only the columns a chunk covers — roughly half the signal stays NaN *by design* — and `serialize.py:241` block-averages it with `.mean()`, not `nanmean`. At n = 626,400 each output cell averages 2,447 columns and so hits at least one NaN; the whole image goes NaN, `_finite_range` returns `None`, the fallback range is `[0,1]`, and `np.clip(nan).astype(uint8)` yields zeros. The pane paints uniform black and reports `value_range: [0,1]`, which is a lie. Invisible to the suite because every test uses a 300-sample synthetic where the block factor is 1. **(ii) The block declares no `max_span_samples`.** On a 2,595,600-sample channel `g_all` alone is 1.33 GB and `compute_morse_wavelet_transform`'s `phi` is 2.66 GB on top; `slice_signal` emits one heavily-overlapping chunk per falling edge, so a noisy real trace transforms far more samples than the signal holds. The four gramian blocks cap themselves at 5,000 samples for a 200 MB matrix; this one caps nothing. Then `_cache_step_result` fires (threshold 1.0 s) and `np.savez_compressed` re-serialises the 1.33 GB mostly-NaN array to disk every run. `discovery.py::_complexity` does not list it as heavy, so no cost warning either. **(iii) `_spanset` has no case for `meta["pseudo_spikes"]`**, so the paper's pseudo-spike set — the main diagnostic when zero spikes is the confirmed answer — never reaches the client. Also `summation_threshold` emits inclusive end indices while `detection.threshold` emits half-open, so span durations from the two differ by one sample. | user; `webui/server/serialize.py:241`, `Adapters/preprocessing_wavelet_transform.py:51-59,90`, `Working/Detection/analysis/dehshibi_detection_analysis.py:139-148,260`, `Working/config.py:162`, `Adapters/detection_summation_threshold.py:88` |
| A3 | **The hierarchical-cluster and classifier blocks need work — diagnosed.** See `04-analyse-training.md` T3 for the full finding. In one line: `catalogue.cluster` throws away everything that makes the clustering *hierarchical* (the scipy linkage matrix, `leaf_order`, `df_membership`, `cophenetic_r`, `silhouette` are all computed by `cluster_window_matrix` and dropped by the adapter), so no dendrogram can be drawn and k has no criterion; and `catalogue.classifier`'s joblib is write-only — nothing in the repo ever loads it back — with a single `holdout_accuracy` as its entire evaluation. | user; `Adapters/catalogue_cluster.py:69-79`, `Adapters/catalogue_classifier.py:229,232-235` |
| A4 | **The demo chain was never removed.** `demo.*` blocks, `DEMO_TEMPLATES` and `?template=drop_motifs9&state=…` still back 40 smoke states and the block-page concept pages, so `?state=running\|failed` come from a simulation rather than a real run. Estimated 1.5 days to replace with live block pages keyed on the new blocks' `meta`. | wiring `reports/01-analyse-blocks.md` Left; `webui/client/src/analyse/index.tsx:27` |
| A5 | **[FIXED — fixup-a 11, `a9370a7`.** `RunState.cancelRequestedAt` is set when the POST is accepted and by the `cancel_requested` SSE frame, so a cancel from another tab shows too; the button reads `Cancelling…` and is disabled with the reason, and the footer says the current stage is still running and that the stages already done are kept. Both pages that render the button.**] The cancel button had no "cancelling…" state (critic P1-6). | wiring `reports/01-analyse-blocks.md` critics |
| A6 | **`autoparams.py` never became the blocks' `recommend` hook** (2 h, declared not done), so the drop template's defaults are the reference span's hard numbers rather than derived per source. | wiring `reports/01-analyse-blocks.md` questions table |
| A7 | **Eleven fixture-era glyph keys in `BY_NAME` name no adapter.** Harmless; the demo pages use them. | wiring `reports/01-analyse-blocks.md` critics |
| A8 | **Two items Discovery filed to this workspace and nobody added**: `detection.seed_matches` runs under `stumpy.match`'s m/4 trivial-match guard with no parameter to set it (§7.6 specifies m/2), and it declares no cost estimator, so any plan containing it costs 0 s and routes local. | wiring `requests/04-to-01.md` |
| A9 | **Unfinished algorithms, with wiring's own recommendation**: the wavelet energy-peak detector (4–6 h, *finish*), `fusion_prediction` as a `model_kind` parameter (2 h), the `comparison` stage as a route not a block, FitzHugh–Nagumo (2–3 days, *ignore for v1*), `compute_spike_statistics` (2 h — but see `00-cross-cutting.md` X3, which makes it load-bearing rather than optional). | wiring `reports/01-analyse-blocks.md` unfinished-algorithms table |
| A10 | `api/analyse.ts` still holds 6 fixture reads. | wiring `reports/05-review.md` §7 |

## Goal · Work · Testing and critique · Report

*(written after the questions)*
