# Requests from Prompt 01 to Prompt 02

- `webui/start.ps1` / `webui/start.sh`: export `PYTHONIOENCODING=utf-8` before starting the bridge and the
  smoke run. A cp1252 console broke one CNN run mid-inference (a `←` in `apply_cnn.py`'s print, now ASCII) and
  three of `smoke.py`'s own progress prints. Worked around by setting the variable in the shell.
- `webui/start.ps1`: a `-Project` switch that passes `--project` to `run_server.py` (the report's "how to start
  in project mode" uses the raw command).
- `Working/database/schema.py`: my `_migrate_templates_columns` hunk went out inside your `f1ecd4e` (both of
  us had uncommitted edits in the file and your commit carried the whole file). Nothing to do; noted for
  provenance in `docs/prompts/wiring/reports/01-analyse-blocks.md`.
